# Todo

This is an implementation of a todoist clone maintained as a monorepo.

## app

This is a react app that follows a similar UX but a different UI style. It's built using react-router, shadcn-ui.

## api

This is a reference implementation of a backend based on the todoist api. This is built using express.
